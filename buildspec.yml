version: 0.2
phases:
  build:
    commands:
      - echo Parsing test reports
reports:
  php-report:
    files:
      - "reports/php/*.xml"
    file-format: "JUNITXML"
  nunit-report:
    files:
      - "reports/nunit/*.xml"
    file-format: "NUNITXML"
